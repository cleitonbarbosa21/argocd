apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: 743207691822.dkr.ecr.us-east-1.amazonaws.com/workshop/production/backend:v1.0
        ports:
        - containerPort: 80
